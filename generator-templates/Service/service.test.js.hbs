import {{pascalCase name}} from "./{{pascalCase name}}";

describe("{{pascalCase name}}", () => {
  it("returns {{pascalCase name}} for getName()", () => {
    expect({{pascalCase name}}.getName()).toBe("{{pascalCase name}}");
  });
});
